# Changelog

## v1.5.0 - Nutrition Experience Improvements

本版本聚焦降低每日记录成本，并让统计和营养建议更贴近日常使用。

### 新增

- 首页新增「最近吃过」，从最近 7 天记录中去重展示最近和高频食物，点击后进入现有确认流程。
- 首页新增「今天还能吃什么」短建议，根据当天剩余热量和三大营养素情况生成，并复用现有 AI 营养教练入口。
- 统计页新增 7 天平均摄入、30 天平均摄入、平均热量缺口 / 盈余、蛋白质达标率和连续记录天数。
- AI 营养教练根据当前时间和已记录餐次动态生成下一餐推荐：早餐后优先午餐，午餐后推荐晚餐，晚餐后展示今日总结。

### 兼容性与稳定性

- 保持文字输入、语音输入、拍照识别、dailyRecords 和 AI 营养教练核心接口不变。
- 统计指标复用现有 `daily_records` 范围查询，不新增数据库集合或云函数。
- 补充最近食物、近期统计和 mealContext 自动化测试；正式环境移除临时诊断日志。

## v1.4.0 - Food Vision

发布日期：2026-08-12

本版本新增拍照和相册识别食物能力，继续复用现有确认页与 `dailyRecords` 保存链路。

### 新增

- 支持拍照识别食物和从相册选择食物图片。
- 客户端上传前压缩图片，控制最长边约 1280px，JPEG quality 为 70。
- 使用 CloudBase 临时文件上传，调用 `recognizeFoodImage` 时只传 `fileID` 和 MIME 类型，避免大 Base64 触发请求大小限制。
- 使用腾讯云 TokenHub 的 YT-VITA 多模态模型识别单个或多个可见食物。
- 返回食物名称、估算份量、单位、热量、蛋白质、碳水、脂肪和置信度。
- 识别结果进入确认页，用户可以修改名称、份量和营养数据，删除误识别项后再确认保存。
- 多种食物作为独立条目写入当天饮食记录，并由 `dailyRecords` 自动汇总热量和三大营养素。

### 隐私与稳定性

- 图片仅用于本次识别，不写入 `daily_records` 或 `nutrition_chat_records`。
- 云函数在识别成功或失败后尝试删除临时图片。
- `VITA_API_KEY` 仅从云函数环境变量读取，不进入前端、日志或 Git。
- 图片上传失败、非食物图片和识别异常均使用用户友好提示，不展示云函数内部错误。

### 兼容性

- 保持文字输入、语音输入、AI 营养教练、私人营养顾问、历史记录、统计和用户档案流程不变。

## v1.3.0 - AI Nutrition Coach

发布日期：2026-08-07

本版本完成 AI 营养教练三阶段能力，并以用户确认后的饮食记录为唯一数据来源。

### 新增

- 新增今日营养分析：结合身体档案、当天摄入、运动、热量缺口和三大营养素生成个性化评价。
- 新增智能饮食推荐：根据剩余热量和蛋白质缺口提供晚餐方案，可确认后加入今日记录。
- 新增私人营养顾问：支持饮食咨询、食物调整、运动建议和明日计划的连续对话。
- 新增 `nutritionCoach` 与 `nutritionChat` 云函数，复用现有 DeepSeek Provider 配置。
- 新增 `nutrition_chat_records` 集合，按用户隔离保存用户和助手消息上下文。

### 安全与稳定性

- `nutrition_chat_records` 设置为 `ADMINONLY`，小程序端不能直接读写聊天记录。
- 移除前端原始错误对象、语音转写身份诊断和凭证状态日志。
- 云函数错误日志统一保留错误代码、友好说明和请求 ID；不返回堆栈、密钥或完整请求参数。
- 营养教练、聊天、语音输入和日常记录完成自动化安全回归。

### 兼容性

- 未改变 `dailyRecords`、`parseDailyInput`、`parseRecord`、`speechToText`、`userProfile` 的成功调用接口与数据归属；语音失败响应已收敛为安全的友好错误。
- 营养推荐继续通过既有 `dailyRecords` 合并保存逻辑写入当天记录。

## v1.2.0 - Voice Input (In Development)

发布日期：待定

本阶段聚焦每日记录效率，为首页新增中文饮食和运动语音输入，同时完整复用已有文本解析、确认和保存流程。

### 新增

- 首页输入区新增麦克风、相机入口、文字输入框和提交按钮。
- 支持点击开始/结束录音，以及长按、松开结束和上滑取消。
- 支持最长 45 秒录音、录音计时、动态状态和转写反馈。
- 新增 `speechToText` 云函数，通过腾讯云一句话识别将录音转换为中文文字。
- 转写结果自动填入现有输入框，可继续编辑，不会自动解析或保存。
- 增加麦克风授权拒绝、录音过短、取消、网络失败、超时和空结果处理。

### 隐私与安全

- 腾讯云 API 密钥只从云函数环境变量读取。
- 原始语音仅用于本次转写，不写入云数据库或长期云存储。
- 云函数日志不记录音频 Base64、转写正文或密钥。
- 文字输入在用户拒绝麦克风权限后仍可正常使用。

### 兼容性

- `parseDailyInput({ text, profile })` 接口保持不变。
- 确认页、`dailyRecords`、数据库结构、历史页和统计页逻辑保持不变。
- 相机入口本阶段仅作下一阶段功能提示，尚未调用图片识别。

## v1.1.1 - WeChat Review Positioning

发布日期：2026-07-13

本版本面向微信体验版审核前的产品定位调整，将用户可见表达统一为「饮食热量记录工具」。

### 优化

- 产品名称调整为「热量记录助手」。
- 将识别按钮和错误提示统一调整为「智能识别」。
- 将首页建议模块调整为「今日建议」。
- 将输入框提示改为「输入今天的饮食或运动记录...」。
- 我的页面不再展示具体模型名称，改为识别服务状态。
- README、Roadmap 和公开发布说明统一为健康记录工具定位。

### 技术说明

- 本版本仅调整用户可见文案、项目简介配置和对应测试断言。
- 未修改云函数、数据库、识别服务调用、保存/查询/删除业务逻辑。

## v1.1.0 - Apple Health UI V2

发布日期：2026-07-10

本版本聚焦视觉系统、页面体验和信息层级重构，将「热量记录助手」统一升级为 Apple Health / Gentler Streak 风格的 iOS 高级简约界面。

### 新增

- 建立 Apple HIG Design System，统一颜色、字体、圆角、阴影、间距、按钮、输入框、卡片、图表和空状态规范。
- 首页新增以「今日热量缺口」为核心的 Today's Goal 卡片。
- 首页新增固定智能输入框。
- 首页今日记录采用 Timeline 样式展示。
- 统计页支持 Apple Health 风格原生趋势图展示。
- 我的页面采用 Apple 设置风格的信息分组与数据卡片。

### 优化

- 首页 Apple Health 风格重构，突出热量缺口、目标进度、营养素和今日建议。
- 历史页重构为日期时间线风格，强化每日饮食、运动和热量汇总阅读体验。
- 统计页重构为数据优先的趋势分析界面，统一 7 天 / 30 天切换体验。
- 我的页面重构为轻量、克制、留白充足的个人健康档案。
- 识别结果确认页重构为清晰的编辑体验。
- 热量缺口展示优化，支持正负状态、长数字自适应字号和视觉状态表达。
- 智能输入框固定逻辑优化，输入框移出滚动内容并固定在页面根级，键盘弹起时跟随上移。
- 页面加载、空数据、错误状态视觉统一。
- UI 统一规范，减少页面之间的风格差异。

### 技术说明

- 本版本仅重构小程序前端 WXML / WXSS / 必要展示 JS。
- 未修改云函数、数据库结构、识别服务 Provider、保存/查询/删除业务逻辑。
- 保持原生微信小程序开发方式，不引入第三方组件库或图表库。
